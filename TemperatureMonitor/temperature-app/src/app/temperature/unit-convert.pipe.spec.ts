import { UnitConvertPipe } from './unit-convert.pipe';

describe('UnitConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
